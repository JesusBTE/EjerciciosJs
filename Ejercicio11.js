//11. Mapear un array

//Definimos un arreglo de números

let numeros = [1, 2, 3, 4, 5];

//Usamos el método map() para elevar cada número al cuadrado

let mapeo = numeros.map((numeros) => Math.pow(numeros, 2));

//Imprimimos el resultado en consola

console.log("Mapeo de números: " + mapeo);
