//7. Recorriendo arreglos con forEach

//Definimos un arreglo de números

let numeros = [2, 4, 6, 8, 10];

//Usamos forEach para recorrer e imprimir cada número del arreglo

numeros.forEach((numeros) => console.log(numeros));
